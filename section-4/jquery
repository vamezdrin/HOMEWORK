Поиск среди пакетов по ключевому слову highlight
apt search highlight

Установка пакета:
sudo apt install -f libjs-jquery-jush

PS. Пришлось обратно включить репозиторий, отключенный на предыдущем шаге, иначе не находил