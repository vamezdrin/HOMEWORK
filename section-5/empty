Нашел файлы, в которых содержался какой-то текст командой:
grep -r '.' ~/empty/

Можно было попробовать найти командой:
sudo find ~/empty/ -type f -not -empty
Но эта команда не сработала, так как все файлы имеют размер 24 байта  